#include <iostream>
using namespace std;

int square(int x)
{
    return (x*x);
}

int cube(int x)
{
    return (x*x*x);
}


int main() {

    int n;
    cin>>n;
    
    cout<<square(n)<<endl;
    cout<<cube(n);

	return 0;
}
