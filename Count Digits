#include <iostream>
using namespace std;

int main() {

    int n,m;
    cin>>n>>m;
    int r,count=0;
    while(n>0){
        r=n%10;
        if(m==r) count++;
        n/=10;
    }
    cout<<count;

	return 0;
}
