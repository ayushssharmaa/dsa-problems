#include <iostream>
using namespace std;

int main() {

    int i,n,sum=0, nums;
    float avg;
    cin>>n;
    for(i=1; i<=n; i++){
        cin>>nums;
        sum=sum+nums;
    }
    avg=sum/(float)n;
    
    cout<<"Sum= "<<sum<<"\n"<<"Avg= "<<avg;
	
	return 0;
}
