#include <iostream>
#include <cmath>
using namespace std;

int main() {

    int i,n;
    cin>>n;
    
    for(i=1;i<=sqrt(n);i++){
        if(n%i==0) {
            cout<<"Not prime";
            exit(0);
        }
    }
    cout<<"Prime";

	return 0;
}
