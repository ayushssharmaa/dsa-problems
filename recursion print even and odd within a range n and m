
        // recursion print even and odd within a range n and m

#include <iostream>
using namespace std;

void f(int a, int b){
    // base case
    if(a>b) return;
    // rec case
    if(a%2!=0) cout<<a<<" ";
    f(a+1,b);
}

int main() {

    int a,b;
    cin>>a>>b;
    f(a,b);

	return 0;
}
