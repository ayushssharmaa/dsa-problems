#include <iostream>
#include <cmath>
using namespace std;

int main() {

    int i,n;
    cin>>n;
    bool flag=1;
    
    for(i=2;i<=sqrt(n);i++){
        if(n%i==0) flag=0;
        //else flag=1;
    }
    if(flag==1) cout<<"No is prime";
    else cout<<"No is not prime";
        
	return 0;
}
